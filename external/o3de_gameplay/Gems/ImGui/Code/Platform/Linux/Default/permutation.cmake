#
# Copyright (c) Contributors to the Open 3D Engine Project.
# For complete copyright and license terms please see the LICENSE at the root of this distribution.
#
# SPDX-License-Identifier: Apache-2.0 OR MIT
#
#

# Generated by O3DE

ly_add_target(
    NAME ImGui.imguilib IMPORTED SHARED
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE
            IMGUI_ENABLED
            $<$<NOT:$<BOOL:FALSE>>:IMGUI_API_IMPORT>
            IMGUI_INCLUDE_IMGUI_USER_H
    INCLUDE_DIRECTORIES
        INTERFACE
            ../External/ImGui/v1.82
            Include
    BUILD_DEPENDENCIES
        INTERFACE

    RUNTIME_DEPENDENCIES
        AZ::AzCore
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/ImGui.imguilib_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME ImGui.ImGuiLYUtils IMPORTED STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            
    BUILD_DEPENDENCIES
        INTERFACE
            Gem::ImGui.imguilib
            Legacy::CryCommon
    RUNTIME_DEPENDENCIES
        Gem::ImGui.imguilib
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/ImGui.ImGuiLYUtils_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME ImGui.Static IMPORTED STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            Gem::ImGui.ImGuiLYUtils
            Gem::LmbrCentral
    RUNTIME_DEPENDENCIES
        Gem::LmbrCentral
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/ImGui.Static_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME ImGui IMPORTED GEM_MODULE
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE

    RUNTIME_DEPENDENCIES
        Gem::ImGui.Static
        Gem::LmbrCentral
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/ImGui_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME ImGui.Editor IMPORTED GEM_MODULE
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE

    RUNTIME_DEPENDENCIES
        Gem::ImGui.Static
        Gem::LmbrCentral.Editor
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/ImGui.Editor_${config}.cmake" OPTIONAL)
endforeach()

if(NOT TARGET ImGui.Clients)
   ly_create_alias( NAME ImGui.Clients NAMESPACE Gem TARGETS Gem::ImGui)
endif()
if(NOT TARGET ImGui.Servers)
   ly_create_alias( NAME ImGui.Servers NAMESPACE Gem TARGETS Gem::ImGui)
endif()
if(NOT TARGET ImGui.Unified)
   ly_create_alias( NAME ImGui.Unified NAMESPACE Gem TARGETS Gem::ImGui)
endif()
if(NOT TARGET ImGui.Builders)
   ly_create_alias( NAME ImGui.Builders NAMESPACE Gem TARGETS Gem::ImGui.Editor)
endif()
if(NOT TARGET ImGui.Tools)
   ly_create_alias( NAME ImGui.Tools NAMESPACE Gem TARGETS Gem::ImGui.Editor)
endif()
