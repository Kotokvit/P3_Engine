#
# Copyright (c) Contributors to the Open 3D Engine Project.
# For complete copyright and license terms please see the LICENSE at the root of this distribution.
#
# SPDX-License-Identifier: Apache-2.0 OR MIT
#
#

# Generated by O3DE

ly_add_target(
    NAME LyShine.Static IMPORTED STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            Gem::Atom_RPI.Public
            Gem::Atom_Utils.Static
            Gem::CommonFeaturesAtom.Static
            Gem::Atom_Bootstrap.Headers
            Gem::AtomFont
            Gem::TextureAtlas
            Legacy::CryCommon
            Gem::LmbrCentral
    RUNTIME_DEPENDENCIES
        Legacy::CryCommon
        Gem::LmbrCentral
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Android/Monolithic/LyShine.Static_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME LyShine IMPORTED GEM_STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            Gem::Atom_RHI.Reflect
            Gem::Atom_RPI.Public
            Gem::LyShine.Static
            Legacy::CryCommon
            Gem::LmbrCentral.API
    RUNTIME_DEPENDENCIES
        Gem::LyShine.Static
        Legacy::CryCommon
        Gem::LmbrCentral.API
        Gem::LmbrCentral
        Gem::TextureAtlas
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Android/Monolithic/LyShine_${config}.cmake" OPTIONAL)
endforeach()

if(NOT TARGET LyShine.Clients)
   ly_create_alias( NAME LyShine.Clients NAMESPACE Gem TARGETS Gem::LyShine)
endif()
if(NOT TARGET LyShine.Servers)
   ly_create_alias( NAME LyShine.Servers NAMESPACE Gem TARGETS Gem::LyShine)
endif()
if(NOT TARGET LyShine.Unified)
   ly_create_alias( NAME LyShine.Unified NAMESPACE Gem TARGETS Gem::LyShine)
endif()
if(NOT TARGET LyShine.Clients.API)
   ly_create_alias( NAME LyShine.Clients.API NAMESPACE Gem TARGETS Gem::LyShine.Clients)
endif()
if(NOT TARGET LyShine.Servers.API)
   ly_create_alias( NAME LyShine.Servers.API NAMESPACE Gem TARGETS Gem::LyShine.Servers)
endif()
if(NOT TARGET LyShine.Unified.API)
   ly_create_alias( NAME LyShine.Unified.API NAMESPACE Gem TARGETS Gem::LyShine.Unified)
endif()
