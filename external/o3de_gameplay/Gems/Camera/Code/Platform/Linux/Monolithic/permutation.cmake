#
# Copyright (c) Contributors to the Open 3D Engine Project.
# For complete copyright and license terms please see the LICENSE at the root of this distribution.
#
# SPDX-License-Identifier: Apache-2.0 OR MIT
#
#

# Generated by O3DE

ly_add_target(
    NAME Camera.Static IMPORTED STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            
    BUILD_DEPENDENCIES
        INTERFACE
            Gem::Atom_RPI.Public
            AZ::AtomCore
            AZ::AzCore
            AZ::AzFramework
    RUNTIME_DEPENDENCIES

    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Monolithic/Camera.Static_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME Camera IMPORTED GEM_STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            
    BUILD_DEPENDENCIES
        INTERFACE
            Gem::Camera.Static
    RUNTIME_DEPENDENCIES
        Gem::Camera.Static
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Monolithic/Camera_${config}.cmake" OPTIONAL)
endforeach()

if(NOT TARGET Camera.Clients)
   ly_create_alias( NAME Camera.Clients NAMESPACE Gem TARGETS Gem::Camera)
endif()
if(NOT TARGET Camera.Servers)
   ly_create_alias( NAME Camera.Servers NAMESPACE Gem TARGETS Gem::Camera)
endif()
if(NOT TARGET Camera.Unified)
   ly_create_alias( NAME Camera.Unified NAMESPACE Gem TARGETS Gem::Camera)
endif()
