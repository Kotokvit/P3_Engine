#
# Copyright (c) Contributors to the Open 3D Engine Project.
# For complete copyright and license terms please see the LICENSE at the root of this distribution.
#
# SPDX-License-Identifier: Apache-2.0 OR MIT
#
#

# Generated by O3DE

ly_add_target(
    NAME GameState.Static IMPORTED STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            AZ::AzCore
    RUNTIME_DEPENDENCIES

    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Monolithic/GameState.Static_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME GameState IMPORTED GEM_STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            Gem::GameState.Static
    RUNTIME_DEPENDENCIES
        Gem::GameState.Static
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Monolithic/GameState_${config}.cmake" OPTIONAL)
endforeach()

if(NOT TARGET GameState.Clients)
   ly_create_alias( NAME GameState.Clients NAMESPACE Gem TARGETS GameState)
endif()
if(NOT TARGET GameState.Servers)
   ly_create_alias( NAME GameState.Servers NAMESPACE Gem TARGETS GameState)
endif()
if(NOT TARGET GameState.Unified)
   ly_create_alias( NAME GameState.Unified NAMESPACE Gem TARGETS GameState)
endif()
