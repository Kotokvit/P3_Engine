#
# Copyright (c) Contributors to the Open 3D Engine Project.
# For complete copyright and license terms please see the LICENSE at the root of this distribution.
#
# SPDX-License-Identifier: Apache-2.0 OR MIT
#
#

# Generated by O3DE

ly_add_target(
    NAME VirtualGamepad.Static IMPORTED STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            AZ::AzCore
            AZ::AzFramework
            Legacy::CryCommon
            Gem::LyShine.Clients.API
    RUNTIME_DEPENDENCIES

    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Monolithic/VirtualGamepad.Static_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME VirtualGamepad IMPORTED GEM_STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            Gem::VirtualGamepad.Static
    RUNTIME_DEPENDENCIES
        Gem::VirtualGamepad.Static
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Monolithic/VirtualGamepad_${config}.cmake" OPTIONAL)
endforeach()

if(NOT TARGET VirtualGamepad.Clients)
   ly_create_alias( NAME VirtualGamepad.Clients NAMESPACE Gem TARGETS Gem::VirtualGamepad Gem::LyShine.Clients)
endif()
if(NOT TARGET VirtualGamepad.Tools)
   ly_create_alias( NAME VirtualGamepad.Tools NAMESPACE Gem TARGETS Gem::VirtualGamepad Gem::LyShine.Tools)
endif()
if(NOT TARGET VirtualGamepad.Builders)
   ly_create_alias( NAME VirtualGamepad.Builders NAMESPACE Gem TARGETS Gem::VirtualGamepad Gem::UiBasics.Builders Gem::LyShine.Builders)
endif()
