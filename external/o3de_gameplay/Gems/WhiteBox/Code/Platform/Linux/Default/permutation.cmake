#
# Copyright (c) Contributors to the Open 3D Engine Project.
# For complete copyright and license terms please see the LICENSE at the root of this distribution.
#
# SPDX-License-Identifier: Apache-2.0 OR MIT
#
#

# Generated by O3DE

ly_add_target(
    NAME WhiteBox.Static IMPORTED STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            AZ::AzCore
            Gem::Atom_RPI.Public
            Gem::Atom_Feature_Common.Public
            Gem::CommonFeaturesAtom.Static
            Legacy::CryCommon
    RUNTIME_DEPENDENCIES

    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/WhiteBox.Static_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME WhiteBox IMPORTED GEM_MODULE
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE

    RUNTIME_DEPENDENCIES
        Gem::WhiteBox.Static
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/WhiteBox_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME WhiteBox.Editor.Static IMPORTED STATIC
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE
            WHITE_BOX_EDITOR
    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE
            AZ::AzToolsFramework
            Legacy::CryCommon
            Legacy::EditorCommon
            Legacy::Editor.Headers
            Gem::WhiteBox.Static
            3rdParty::OpenMesh
    RUNTIME_DEPENDENCIES

    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/WhiteBox.Editor.Static_${config}.cmake" OPTIONAL)
endforeach()

# Generated by O3DE

ly_add_target(
    NAME WhiteBox.Editor IMPORTED GEM_MODULE
    NAMESPACE Gem
    COMPILE_DEFINITIONS
        INTERFACE

    INCLUDE_DIRECTORIES
        INTERFACE
            Include
    BUILD_DEPENDENCIES
        INTERFACE

    RUNTIME_DEPENDENCIES
        Gem::WhiteBox.Editor.Static
    TARGET_PROPERTIES

)



set(configs debug;profile;release)
foreach(config ${configs})
    include("Platform/Linux/Default/WhiteBox.Editor_${config}.cmake" OPTIONAL)
endforeach()

if(NOT TARGET WhiteBox.Clients)
   ly_create_alias( NAME WhiteBox.Clients NAMESPACE Gem TARGETS Gem::WhiteBox)
endif()
if(NOT TARGET WhiteBox.Servers)
   ly_create_alias( NAME WhiteBox.Servers NAMESPACE Gem TARGETS Gem::WhiteBox)
endif()
if(NOT TARGET WhiteBox.Unified)
   ly_create_alias( NAME WhiteBox.Unified NAMESPACE Gem TARGETS Gem::WhiteBox)
endif()
if(NOT TARGET WhiteBox.Tools)
   ly_create_alias( NAME WhiteBox.Tools NAMESPACE Gem TARGETS Gem::WhiteBox.Editor)
endif()
if(NOT TARGET WhiteBox.Builders)
   ly_create_alias( NAME WhiteBox.Builders NAMESPACE Gem TARGETS Gem::WhiteBox.Editor)
endif()
